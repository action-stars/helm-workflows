name: Publish Release

on:
  push:
    tags:
      - v*

jobs:
  workflow:
    name: Workflow
    uses: action-stars/generic-workflows/.github/workflows/release.yaml@d4724b3ea1635d9cc7064f5302cc411e54d52334 # v0.9.0
    permissions:
      contents: write
