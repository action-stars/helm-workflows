name: Publish Release

on:
  push:
    tags:
      - v*

jobs:
  workflow:
    name: Workflow
    uses: action-stars/generic-workflows/.github/workflows/release.yaml@38584cb405d18a37a34a267f18ded72214d0165a # v0.10.0
    permissions:
      contents: write
