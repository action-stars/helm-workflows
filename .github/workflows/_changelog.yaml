name: Dependabot Changelog Entry

on:
  pull_request:
    types: [opened, reopened, synchronize, labeled]
    branches: [main]

permissions: read-all

jobs:
  workflow:
    name: Workflow
    uses: action-stars/generic-workflows/.github/workflows/dependabot-changelog.yaml@9ebbb6e9360565432fd1898fda091048be02db1b # v0.12.0
    permissions:
      contents: read
      id-token: write
