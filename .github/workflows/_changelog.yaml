name: Dependabot Changelog Entry

on:
  pull_request:
    types: [opened, reopened, synchronize, labeled]
    branches: [main]

permissions: read-all

jobs:
  workflow:
    name: Workflow
    uses: action-stars/generic-workflows/.github/workflows/dependabot-changelog.yaml@436b5d8ebbc67151ec77f214d667d7393c3916af # v0.11.0
    permissions:
      contents: read
      id-token: write
